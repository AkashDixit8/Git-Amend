name: my first workflow

on: 
  push:
   branches: [main]

jobs:
  show:
     runs-on: ubuntu-latest


     steps:
       - name: display
         run: echo "hello this is my first workflow"
         
     
      
    
   
